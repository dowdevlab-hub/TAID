# Optional only. The current SAMPLE prototype runs without environment variables.
# If you test the optional AI route, keep this server-only and never use NEXT_PUBLIC_.
OPENAI_API_KEY=

# Optional model override for the AI route.
OPENAI_MODEL=gpt-5-mini
